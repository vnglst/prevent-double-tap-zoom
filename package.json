{
  "name": "prevent-double-tap-zoom",
  "version": "2.0.4",
  "description": "No zooming on mobile when user double taps on a button/screen.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "release": "yarn build && np",
    "test": "echo \"Ok: no test specified\" && exit 0"
  },
  "keywords": [],
  "author": "Koen van Gilst <koen@koenvangilst.nl> (https://koenvangilst.nl)",
  "license": "MIT",
  "dependencies": {
    "typescript": "^2.9.2"
  },
  "devDependencies": {
    "np": "^3.0.4"
  }
}
